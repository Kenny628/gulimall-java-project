/*
 * Created on 2024-05-02 ( 14:45:36 )
 * Generated by Telosys ( https://www.telosys.org/ ) version 3.3.0
 */
package com._yzhheng.rest.services;

import java.util.List;
import java.util.Optional;


import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Service;

import com._yzhheng.persistence.entities.PmsSpuInfoDesc;
import com._yzhheng.persistence.repositories.PmsSpuInfoDescRepository;
import com._yzhheng.rest.dto.PmsSpuInfoDescDTO;
import com._yzhheng.rest.services.commons.GenericService;

/**
 * REST service for entity "PmsSpuInfoDesc" <br>
 * 
 * This service provides all the necessary operations required by the REST controller <br>
 * 
 * @author Telosys
 *
 */
@Service
public class PmsSpuInfoDescService extends GenericService<PmsSpuInfoDesc, PmsSpuInfoDescDTO> {

	private static final Logger logger = LoggerFactory.getLogger(PmsSpuInfoDescService.class);

	private final PmsSpuInfoDescRepository repository; // injected by constructor

	/**
	 * Constructor (usable for Dependency Injection)
	 * 
	 * @param repository the repository to be injected
	 */
	public PmsSpuInfoDescService(PmsSpuInfoDescRepository repository) {
		super(PmsSpuInfoDesc.class, PmsSpuInfoDescDTO.class);
		this.repository = repository;
	}
	
	/**
	 * Returns the entity ID object from the given DTO
	 *
	 * @param dto
	 * @return
	 */
	private Long getEntityId(PmsSpuInfoDescDTO dto) {
		return dto.getSpuId();
	}

	/**
	 * Finds all occurrences of the entity
	 *
	 * @return
	 */
	public List<PmsSpuInfoDescDTO> findAll() {
		logger.debug("findAll()");
		Iterable<PmsSpuInfoDesc> all = repository.findAll();
		return entityListToDtoList(all);
	}

	/**
	 * Finds the entity identified by the given PK
	 *
	 * @param spuId 
	 * @return the entity or null if not found
	 */
	public PmsSpuInfoDescDTO findById(Long spuId) {
		Long entityId = spuId;
		logger.debug("findById({})", entityId);
		Optional<PmsSpuInfoDesc> optionalEntity = repository.findById(entityId);
		return entityToDto(optionalEntity);
	}

	/**
	 * Saves the given entity with the given PK <br>
	 * "UPSERT" operation (updated if it exists or created if it does not exist)
	 *
	 * @param spuId 
	 * @param dto 
	 */
	public void save(Long spuId, PmsSpuInfoDescDTO dto) {
		Long entityId = spuId;
		logger.debug("save({},{})", entityId, dto);
		// force PK in DTO (just to be sure to conform with the given PK) 
		dto.setSpuId(spuId);
		repository.save(dtoToEntity(dto));
	}

	/**
	 * Updates the given entity if it exists
	 *
	 * @param dto
	 * @return true if updated, false if not found
	 */
	public boolean update(PmsSpuInfoDescDTO dto) {
		logger.debug("update({})", dto);
		if (repository.existsById(getEntityId(dto))) {
			repository.save(dtoToEntity(dto));
			return true; // find and updated
		} else {
			return false; // not found (not updated)
		}
	}

	/**
	 * Updates partially the given entity if it exists
	 *
	 * @param spuId 
	 * @param dto
	 * @return true if updated, false if not found
	 */
	public boolean partialUpdate(Long spuId, PmsSpuInfoDescDTO dto) {
		Long entityId = spuId;
		logger.debug("partialUpdate({}, {})", entityId, dto);
		Optional<PmsSpuInfoDesc> optionalEntity = repository.findById(entityId);
		if (optionalEntity.isPresent()) {
			PmsSpuInfoDesc entity = optionalEntity.get();
			// implement here the partial update
			// entity.setXxx(dto.getXxx());
			// etc ...
			repository.save(entity);
			return true; // find and updated
		} else {
			return false; // not found (not updated)
		}
	}

	/**
	 * Creates the given entity
	 *
	 * @param dto
	 * @return true if created, false if already exists
	 */
	public boolean create(PmsSpuInfoDescDTO dto) {
		logger.debug("create({})", dto);
		if (repository.existsById(getEntityId(dto))) {
			return false; // already exists, not created
		} else {
			repository.save(dtoToEntity(dto));
			return true; // created
		}
	}

	/**
	 * Deletes an entity by its PK
	 *
	 * @param spuId 
	 * @return true if deleted, false if not found
	 */
	public boolean deleteById(Long spuId) {
		Long entityId = spuId;
		logger.debug("deleteById({})", entityId);
		if (repository.existsById(entityId)) {
			repository.deleteById(entityId);
			return true; // find and deleted
		} else {
			return false; // not found (not deleted)
		}
	}

	// -----------------------------------------------------------------------------------------
	// Specific "finders"
	// -----------------------------------------------------------------------------------------
/***
	public List<PmsSpuInfoDescDTO> findByTitle(String title) {
		logger.debug("findByTitle({})", title);
		// List<PmsSpuInfoDesc> list = repository.findByTitle(title);
		List<PmsSpuInfoDesc> list = repository.findByTitleContaining(title);
		return entityListToDtoList(list);
	}

	public List<PmsSpuInfoDescDTO> findByPrice(BigDecimal price) {
		logger.debug("findByPrice({})", price);
		// List<PmsSpuInfoDesc> list = repository.findByTitle(title);
		List<PmsSpuInfoDesc> list = repository.findByPrice(price);
		return entityListToDtoList(list);
	}

	public List<PmsSpuInfoDescDTO> findByTitleAndPrice(String title, BigDecimal price) {
		logger.debug("findByTitleAndPrice({}, {})", title, price);
		List<PmsSpuInfoDesc> list = repository.findByTitleContainingAndPrice(title, price);
		return entityListToDtoList(list);
	}
***/
}
